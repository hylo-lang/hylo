public trait RandomNumberGenerator {
  /// Returns a value from a uniform, independent distribution of binary data.
  // TODO: change to UInt64 per Swift
  fun next() -> UInt
}

extension RandomNumberGenerator {
  /// Returns a random value that is less than the given upper bound.
  // TODO: Add constraints FixedWidthInteger & UnsignedInteger per swift.
  fun next<T>(upper_bound: T) -> T {
    fatal_error() // TODO: implementation.
  }
}
