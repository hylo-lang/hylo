/// A type whose values can be consumed.
public trait Movable {

  /// Gives `self` the value of a consumed `source`.
  fun take_value(from source: sink Self) -> {self: Self, Void} {
    set
    inout
  }

}
