/// Returns 0 iff it's a right angle triangle.
fun is_right_triangle(a: Int, b: Int, c: Int) -> Int {
  let lhs = (a * a) + (b * b)
  let rhs = c * c
  return lhs - rhs
}

public fun problem(a: Int, b: Int, c: Int) -> Int {
  return is_right_triangle(a: a, b: b, c: c)
}