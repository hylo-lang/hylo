/*! h
    Int factorial(Int n);
    int main();
 */
/*! cpp
      Int factorial(Int n)  {
        return n < 2 ? 1 : n * factorial(n - 1);
      }
 */
fun factorial(_ n: Int) -> Int {
  if n < 2 { 1 } else { n * factorial(n - 1) }
}

public fun main() {
  let _ = factorial(6)
}