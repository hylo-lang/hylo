/*! h
    int factorial(int n);
    int main();
 */
/*! cpp
    int factorial(int n) {
    // fun call TODO(n, 2)
     ? 1 : /* fun call TODO(n, // function call
    ) */}
 */
fun factorial(_ n: Int) -> Int {
  if n < 2 { 1 } else { n * factorial(n - 1) }
}

public fun main() {
  let _ = factorial(6)
}