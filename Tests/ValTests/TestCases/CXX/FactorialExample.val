/*! h
    int factorial(int n);
    int main();
 */
/*! cpp
    int factorial(int n) {
    /* fun call TODO(// l-value with capability
    , // l-value with capability
    ) */ ? // integer literal
     : /* fun call TODO(// l-value with capability
    , // l-value with capability
    ) */}
    int main() {
    (void) // function call
    }
 */
fun factorial(_ n: Int) -> Int {
  if n < 2 { 1 } else { n * factorial(n - 1) }
}

public fun main() {
  let _ = factorial(6)
}