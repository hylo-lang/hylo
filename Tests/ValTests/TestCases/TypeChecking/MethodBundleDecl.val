//! expect-failure

type A {

  fun m0(_ x: Int) -> A {
    let   { A() }
    sink  { A() }
  }

  fun m2(_ x: Int) -> A {
    let   { A() }
    inout {}      //! diagnostic mutating bundle must return '{self: A, A}'
  }

  fun m2(_ x: Int) {
    let   {}
    inout {}      //! diagnostic mutating bundle must return '{self: A, {}}'
  }

}
