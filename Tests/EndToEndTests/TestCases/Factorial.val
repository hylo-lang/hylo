//! expect-success

fun factorial(_ n: Int) -> Int {
  if n < 2 { 1 } else { n * factorial(n - 1) }
}

fun assert(_ b: Bool) {
  if !b {
    let _ = fatal_error()
  }
}

public fun main() {
  let r = factorial(6)
  assert(r == 720)
}
